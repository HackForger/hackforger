---
pr: [192, 193]
commits:
  - 1bca65a814839e8c5d481b723d0992703bf770c1
  - 472073961c662de5414f6e7d2d327d7156066593
  - 6b53b5b416efa19e07ed33a9d30f8b0c06d1ac81
  - 90baa357d6bd3042415e86edf2391216c9ef2f20
  - 5538d823d8
  - 5e3a7f9ffd
tested_against: static landing files + https://www.synnovator.com post-deploy smoke
tested_at: 2026-07-08T17:13:52+0800
e2e_owner: claude
admin_signoff:
  by: h2oslabs-operator
  at: 2026-07-08T17:13:52+0800
  notes: "In-chat production authorization this session: operator requested PR #192 and #193 be merged and re-pushed to production, and explicitly directed that landing-only changes take the fast path (快速通道). Both PRs touch only static landing assets, so the local Mac test-environment redeploy was intentionally skipped — same landing-only fast path used by PR #191/#186/#181. Production deploy still runs the blessed deploy/ecs/redeploy.sh preflight + smoke checks. This is not a human manual walk-through; it is operator authorization plus static verification below."
---

# PR #192 + #193 — S2 排名重排 / S3 赛程时间 / 赛规弹窗 landing update — smoke report

## Scope

Two stacked PRs, both **static landing assets only**. #193 is built on top of
#192 (shares commit `1bca65a814`), so they were merged base-first (#192 → #193).

Files changed (runtime-served custom public path):

- `custom/public/assets/landing/s2-ranking.html`
- `custom/public/assets/landing/index.html`
- new design note `docs/notes/landing-date-driven-buttons.md` (docs, infra-skip)
- screenshot evidence under `docs/tests/e2e/reports/assets/`

No Go code, migrations, locale bindata, `web_src/`, dependency files, or server
configuration changed. The two GitHub merge commits (`5538d823d8`, `5e3a7f9ffd`)
have empty combined diffs and are infra-skip under `preflight.sh`.

### Commit → change map

| SHA | change |
| --- | --- |
| `1bca65a814` | S2: 19 支队伍按 S2 汇总表有效平均分从高到低重排；弹窗删除「对外展示项目名」「平台仓库链接」「演示链接」3 栏，保留「团队/作者」「系统项目名」「项目简介」 |
| `472073961c` | S3: W2/W3/W4 赛程时间更新；年度总决赛 tag 改「待定」；中英文赛规弹窗同步；新增日期驱动按钮状态 (`data-wave-start`/`data-wave-end` + 客户端 JS，服务端 Phase 独立校验) |
| `6b53b5b416` | 修复主时间表遗漏：全球青年培育赛日期更新，年度总决赛「时间待定」中英文同步，i18n DICT 英文翻译同步 |
| `90baa357d6` | 设计说明文档（docs，infra-skip） |

## Pre-deploy static verification

Performed on the merged `v0.1-dev/hackforger` tip `5e3a7f9ffd`.

**S2 ranking modal (s2-ranking.html):**
- Removed columns absent — `对外展示项目名` (0), `平台仓库链接` (0), `演示链接` (0).
- Kept columns present — `团队/作者` (1), `系统项目名` (1), `项目简介` (3).

**S3 schedule + finals + date-driven buttons (index.html):**
- `待定` (TBD finals) present — 11 occurrences (中英文赛规弹窗 + tag).
- Date-driven button hooks present — `data-wave-start` (8), `data-wave-end` (6).
- New S3 dates present in the timetable — 7月23日 / 7月24日 / 8月4日 / 8月12日 / 8月14日.

## Production verification gate

Deployment uses `bash deploy/ecs/redeploy.sh`, which:

- runs `deploy/ecs/preflight.sh` (this report satisfies the gate for the three
  user-facing SHAs above; merges + docs are infra-skip);
- rebuilds the binary (landing-only change → identical binary, but precedent is
  to let it rebuild rather than `--skip-build`);
- rsyncs `custom/templates/` and `custom/public/` to the ECS;
- restarts gitea and records `.last-deploy`.

## Post-deploy smoke (filled after redeploy)

- [ ] https://www.synnovator.com landing returns 200.
- [ ] S2 ranking modal shows the 3-column layout (removed columns gone).
- [ ] S3 timetable shows updated dates; 年度总决赛 shows 待定.
- [ ] Date-driven Wave buttons render correct state for today's date.
